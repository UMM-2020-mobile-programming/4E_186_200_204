<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tubes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".sewa" />
        <activity android:name=".beli" />
        <activity android:name=".sebuahseni" />
        <activity android:name=".power" />
        <activity android:name=".maxhavelaar" />
        <activity android:name=".laskarpelangi" />
        <activity android:name=".koalakumal" />
        <activity android:name=".dilan1991" />
        <activity android:name=".dilan1990" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>